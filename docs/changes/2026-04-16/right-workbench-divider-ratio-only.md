# 右侧工作区中间分割线改为只调比例

**时间**: 2026-04-16 14:01

## 改了什么

1. 去掉了中间分割线拖拽结束后对主窗口 `setBounds` 的同步。
2. 中间分割线现在只更新右侧工作区内部宽度状态，不再改变窗口总宽。

## 为什么改

- 用户期望中间分割线只调左侧内容和右侧工作区的比例。
- 窗口总宽属于系统窗口尺寸，应该只通过窗口边框调整，或在打开/关闭工作区时统一处理。

## 涉及文件

- `src/renderer/src/App.tsx`

## 结果

- 拖动中间分割线时，只会改变内容比例。
- 容器总宽保持不变。
